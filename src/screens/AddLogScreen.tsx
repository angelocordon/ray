import * as React from 'react';
import { Text } from 'react-native';
import { Container } from 'components';

const AddLogScreen: React.FC = () => (
  <Container>
    <Text>Add Log Screen</Text>
  </Container>
);

export default AddLogScreen;
